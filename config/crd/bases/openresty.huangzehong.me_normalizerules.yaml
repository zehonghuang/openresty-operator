---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: normalizerules.openresty.huangzehong.me
spec:
  group: openresty.huangzehong.me
  names:
    kind: NormalizeRule
    listKind: NormalizeRuleList
    plural: normalizerules
    singular: normalizerule
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NormalizeRule is the Schema for the normalizerules API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NormalizeRuleSpec defines the desired state of NormalizeRule
            properties:
              request:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  Request normalization rules
                  Defines inline request normalization logic (JSONPath or Lua)
                type: object
                x-kubernetes-preserve-unknown-fields: true
              response:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  Response normalization rules
                  Defines inline response normalization logic (JSONPath or Lua)
                type: object
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            description: NormalizeRuleStatus defines the observed state of NormalizeRule
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
